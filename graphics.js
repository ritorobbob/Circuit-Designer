var sideBar;
function graphicsSetup() {
    createCanvas(windowWidth, windowHeight);
    background(120);
    noStroke();
    fill(1);
    sideBar = rect(windowWidth - 100, 0, 100, windowHeight);
    
}
